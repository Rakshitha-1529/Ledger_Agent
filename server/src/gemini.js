import { GoogleGenAI } from '@google/genai';

const instruction = `You are an AI accounting document extraction assistant. Analyse OCR text from an invoice, receipt, purchase document, or sales document. Return only valid JSON; never use markdown or explanations. Do not invent values: use null. Monetary values must be numbers without currency symbols or commas. Preserve invoice numbers exactly. Dates must use YYYY-MM-DD when known. Return exactly this JSON shape: {"documentType":"purchase_invoice | sales_invoice | receipt | other","vendor":"string or null","customer":"string or null","invoiceNumber":"string or null","date":"YYYY-MM-DD or null","gstin":"string or null","items":[{"name":"string","quantity":"number or null","unitPrice":"number or null","amount":"number or null"}],"subtotal":"number or null","gst":"number or null","total":"number or null","currency":"string or null","confidence":"number between 0 and 1","validationIssues":["string"]}`;
function parse(text) { const json=text.replace(/^```(?:json)?\s*/i,'').replace(/\s*```$/,'').trim(); const value=JSON.parse(json); if(!value||typeof value!=='object'||Array.isArray(value))throw new Error('Gemini returned an invalid extraction object');return value; }
export async function extractWithGemini(ocrText, file) { if(!process.env.GEMINI_API_KEY)throw new Error('Gemini is not configured. Add GEMINI_API_KEY to server/.env and restart the API.'); const ai=new GoogleGenAI({apiKey:process.env.GEMINI_API_KEY});const response=await ai.models.generateContent({model:'gemini-3.6-flash',contents:[{text:`${instruction}\n\nOCR TEXT:\n${ocrText}`},{inlineData:{mimeType:file.mimeType,data:file.buffer.toString('base64')}}],config:{responseMimeType:'application/json'}});return parse(response.text); }
